{
  "name": "@leetfair/extension",
  "private": true,
  "version": "0.1.0",
  "description": "LeetFair Chrome MV3 extension: opt-in behavioral signal capture for contest sessions",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
