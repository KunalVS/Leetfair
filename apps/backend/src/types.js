/**
 * Canonical event shapes shared by the extension and the backend.
 *
 * The extension produces these; the backend ingests, validates, stores and
 * later aggregates them. Keep this file the source of truth for wire format.
 */
export {};
//# sourceMappingURL=types.js.map